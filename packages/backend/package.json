{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "apollo-server": "^3.6.4",
    "apollo-server-express": "^3.6.4",
    "dotenv": "^16.0.0",
    "ethers": "^5.6.1",
    "express": "^4.17.3",
    "graphql": "^16.3.0",
    "graphql-upload": "^13.0.0",
    "nexus": "^1.3.0",
    "pg": "^8.7.3"
  },
  "scripts": {
    "test": "mocha -r ts-node/register --timeout 20000 'tests/**/*.ts'",
    "dev": "ts-node-dev --transpile-only --no-notify --exit-child src/index.ts",
    "generate": "ts-node --transpile-only src/schema.ts",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/graphql-upload": "^8.0.11",
    "@types/uuid": "^8.3.4",
    "ts-node-dev": "^1.1.8"
  }
}
